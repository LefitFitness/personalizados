# Simulador de Orçamento de Peças Personalizadas

Este é um site simples de simulação de orçamentos, onde o cliente pode selecionar:

- Tipo de peça
- Tipo de tecido
- Adicionais personalizados

O sistema calcula automaticamente o valor final com base nas escolhas feitas.

## ✅ Funcionalidades

- Interface amigável
- Cálculo automático de valor
- Pode ser publicado de graça com GitHub Pages

## 🚀 Como colocar no ar (GitHub Pages)

1. Crie um repositório no GitHub com o nome que quiser (ex: `orcamento-pecas`).
2. Faça upload deste arquivo `index.html` no repositório.
3. Vá em **Settings > Pages** (ou "Pages" na barra lateral).
4. Em **Source**, selecione `Deploy from a branch` e escolha a branch (geralmente `main`) e a pasta `/ (root)`.
5. Clique em “Save”.
6. Pronto! Em poucos segundos/minutos seu site estará disponível no link exibido.

## ✏️ Personalização

Para alterar os valores ou opções:
- Abra o arquivo `index.html`.
- Edite os blocos de `<option>` e os objetos `precosPecas`, `precosTecidos`, `precosAdicionais`.
